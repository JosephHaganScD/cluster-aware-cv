# Cluster-Aware Cross-Validation for Prediction Models with Repeated-Measures Predictors

This repository contains R code supporting the manuscript:

> "Quantifying the Optimism of Naive Cross-Validation for Binary Outcome Prediction with Clustered Repeated-Measures Predictors: A Simulation Study and Clinical Illustration"

## Overview

This project investigates the performance of cross-validation (CV) strategies for prediction models when predictors are measured repeatedly over time but the binary outcome is defined at the subject level. In this setting, standard observation-level CV can introduce data leakage and lead to optimistic estimates of model performance.

## Repository Structure

```
.
├── data/
│   └── final_daily_data.csv    # Input data for clinical illustration (not included)
├── results/
│   ├── simulation/             # Simulation outputs (generated by code)
│   ├── figures/                # Manuscript figures (generated by code)
│   └── rop_analysis/           # Clinical analysis outputs (generated by code)
├── simulation_factorial.R      # Full factorial simulation study
├── sim_figures.R               # Figure generation and summary statistics
├── clinical_analysis.R         # ROP dataset analysis
└── README.md
```
